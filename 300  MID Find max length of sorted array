def sorted(nums):
    d=[]
    for n in nums:
        if not d or n>d[-1]:
           d.append(n)
        else:
           i,j=0,length(d)-1
           while(i<=j):
              mid=(i+j)//2
              if(n>=d[mid]):
                 j=mid-1
              else:
                 i=mid+1
              d[l]=n
     return len(d)
  
