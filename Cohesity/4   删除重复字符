def func(nums):
    if not nums:
       return 0
    s,f=1,1
    while f <len(nums):
          if nums[f]!=nums[f-1]:
             nums[s]=nums[f]
             s+=1
             
    return s   ###结果s已经加了1
    
