a sorted list or array, to find the the sum of two number ==target
def func(arr,t):
    if not arr:
       return False
    i,j=0,len(arr)-1
    while(i<j):
       sum=arr[i]+arr[j]
       if sum>t:
          j-=1
       elif sum<t:
          i+=1
       else:
           return i,j
           
     return False
     
